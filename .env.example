# Optional. If absent, WEBQUANT runs the deterministic demo intelligence provider.
# NOTE: never place private credentials in a VITE_ variable for real production use;
# route live LLM/search calls through a server function instead.
VITE_ENABLE_LIVE=false
